# Infrastructure boundary

Checkpoint 1 local infrastructure is defined by the root `docker-compose.yml` and the
two application Dockerfiles. Cloud-specific deployment, secret stores, gateways, and
production observability are intentionally deferred beyond this checkpoint.

