# NLP and speech adapters

The local deterministic adapter supports English, Marathi, Hindi, and Romanized
fixtures, including negation, duration, severity, ambiguity, source spans, and strict
provider-response validation. The local speech boundary accepts a supplied
transcript and needs no paid credentials. Both are demo adapters; guided fields remain
the authoritative input.
